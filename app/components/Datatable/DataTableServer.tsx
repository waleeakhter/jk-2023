import React from 'react'
import Datatable from './Datatable'

type Props = {}

const DataTableServer = (props: Props) => {
    return (
        <Datatable {...props} />
    )
}

export default DataTableServer